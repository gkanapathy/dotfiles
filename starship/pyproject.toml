[project]
name = "starship-preset-build"
version = "0.1.0"
description = "Merge starship preset stdout with local TOML overlays"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["tomlkit>=0.13"]
